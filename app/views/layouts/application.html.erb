<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="ROBOTS" content="NOODP" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="keywords" content="technical writing jobs; technical marketing jobs; work the docs; read the docs; write the docs;"/>
    <title><%= content_for?(:title) ? yield(:title) : "Excel With Code | Find experienced technical job seekers." %></title>
    <link href='https://fonts.googleapis.com/css?family=Lato:400,700,900' rel='stylesheet' type='text/css'>
    <%= stylesheet_link_tag :application, media: "all" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= render "javascript" %>
    <%= render "payola/transactions/stripe_header" %>
    <%= csrf_meta_tags %>
  </head>
  <body class="<%= body_class %>">
      <%= render "flashes" -%>
      <%= yield %>
  </body>
</html>
